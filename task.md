# Task List — Theme Enhancements & Animations

- [x] Implement grid background overlay in `drone-photography/index.html` structure.
- [x] Add custom CSS HUD / viewfinder animations in `drone-photography/index.html` inline `<style>` block.
- [x] Add dynamic Javascript markup builder in `drone-photography/index.html` script block.
- [x] Verify HUD interactivity, rotation, text contrast, laser lines, and flying grid background.
- [x] Add live loop background video feeds dynamically playing inside cards on hover.
- [x] Integrate high-end camera shutter flash (capture snap) transition on hover start.
- [x] Change the "Book Drone Session" CTA button hover state from black to a premium Gold-Dark bronze color with soft shadow.
- [x] Align contact page info card and form container backgrounds to warm beige, inputs to clean white with gold borders, and text to high-contrast dark for full white theme compliance.
- [x] Add `white-theme` class to `<body>` on the Contact page so the dynamic header/navbar renders in the correct light styling identical to other pages.
- [x] Add `white-theme` class to `<body>` on the Book Now page, align booking form container background to warm beige, form controls to clean white with gold-dark borders, and text labels to high-contrast dark for full white theme compliance.
- [x] Add CSS specificity overrides to `css/style.css` to disable static card styling rules on the Packages page flip card wrappers, enabling clean and smooth 3D flipping transitions on hover.

